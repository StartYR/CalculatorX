# 程序员模式首版

- 状态：首版实现完成，待真机验收
- 类型：功能
- 分支：`codex/programmer-mode`
- 基线：`main` 的 `e9bc1eb`
- 日期：2026-09-23
- 关联：[实施计划](../plan/programmer-mode-implementation.md) · [架构与操作说明](../architecture/programmer.md)

## 变更摘要

进制转换入口新增程序员整数与 IEEE 754 页面。整数支持 8/16/32/64 bit、四进制同步、算术表达式、逻辑与算术移位、旋转/带进位旋转、ADC/SBB、三态标志、逐 bit 编辑和原码/反码/补码观察。IEEE 支持 binary16/32/64、精确 roundTiesToEven、特殊值、字段和 NaN payload 观察。

五列键盘复用 `KeyboardKeySurface`，API 26 且全局开关开启时使用底部 TabBar 的沉浸光感，其他情况使用传统布局。原生长按菜单提供副功能，支持连续退格、复制与粘贴、触感和撤销/重做。

偏好保存最近配置；模块往返保持进程内会话。成功确认的计算写入既有 RDB，支持完整上下文回填。程序员去重会比较扩展配置；同时校正全局历史实际页签和分类索引，使“转换”能够显示 `base` 记录，表结构无需迁移。

## 实现边界

- 整数核心使用 bit 数组，不以 `number` 保存完整 64 位数据，也不进入 CAS/WebView。
- 负字面量不能小于补码最小值；正字面量可输入无符号位模式，再按当前符号解释。
- 乘除/逻辑运算 CF、OF 不适用；旋转只定义 CF；C-in 手动控制，固定语义不模拟 CPU。
- IEEE 显示十进制为简短表示，精确值另以整数乘二进制幂展示。格式转换直接从位模式舍入；跨格式 NaN 规范化，原始位编辑保留 payload。
- 普通数值转换、浮点四则运算和处理器专属行为不在首版范围。版本号和 Changelog 未变更。
- 使用计划中保存的信息层级实现排版，截图原文件在实施时不可访问。

## 验证

| 检查 | 结果 |
| --- | --- |
| `node tools/test-programmer.mjs` | 257,215 项断言通过：各字长极值组合、64 位往返、算术/标志、移位/旋转、binary16 全部非 NaN 位模式、精确舍入、会话与历史恢复 |
| ArkTS `entry@default/debug test` | 8 项通过，0 failure / error，含新增 4 项程序员用例 |
| `tools/check-keyboard-architecture.ps1` | 通过，新模块已纳入清单 |
| `tools/test-programmer-cli.ps1` | 通过，纯 UI 树解析、白名单和场景静态检查 |
| CLI 启动器本地自检、PowerShell AST、文档链接 | 通过；99 个本地 Markdown 文件目标存在 |
| `entry@default/debug assembleHap` | 通过；使用工作树内独立本地配置生成未签名包 |

存在工程既有的废弃 API / C++ 告警，不能以构建成功替代设备结果。本次没有安装到设备，没有执行设备侧 CLI 场景，也没有推送或合并分支。

## 待设备验收

API 26 的传统/沉浸双开关、API 23 回退、深浅色、64 位长值、小窗口/横屏、长按菜单是否重复触发、连续退格、位编辑、历史回填、应用重启设置恢复。原工作树保持在原分支，开发结果位于独立工作树。

## 2026-09-26：显示与交互修复

后续开发位于原目录的 `feature/base-convert` 分支。

- 进制数值文本直接响应点击，行内空白区域继续支持切换输入进制，保留长按复制。
- 整数 64 bit 与 IEEE binary64 的 BIN 按高位在上、低位在下分为两行，每行 32 位；数值统一右对齐，窄窗口按可用宽度缩小字号。
- 模式、检查面板、有符号、C-in 与浮点字长按钮使用响应式属性同步文字和高亮；标志位数值也同步刷新。
- 页面纵向滚动启用原生弹性边缘效果。

验证：`entry@default/debug assembleHap`、键盘架构检查和差异空白检查通过；核心与会话测试共 257,251 项断言通过，新增 8/64 位符号切换、原始位模式保持及切换后的除法样例。构建仍有废弃 API 等工程告警。

待真机检查：数字本体点击与长按复制、两行 BIN 在窄窗口及大字体下的完整性、各标签高亮、滚动回弹，以及 API 26 沉浸键盘与 API 23 传统回退。本轮未安装或操作设备。
