// entry/src/main/ets/utils/ApiConfig.template.ets

/**
 * 注意：这是一个模板文件。
 * 请将此文件重命名为 ApiConfig.ets，并将下方的值替换为你自己的真实 Token。
*/

export class ApiConfig {
  static readonly EXCHANGE_API_TOKEN: string = 'YOUR_API_TOKEN_HERE';
}